<li class="list-group-item", id="<%= feed_item.id %>">
	
	<strong><%= feed_item.address %></strong>,
		  <%= feed_item.city %>,
		  <%= IsoCountryCodes.find(feed_item.country).name %>
	
	<span class="description">Description:<%= feed_item.description %></span>
	<span class="price">Price:<%= feed_item.price %>€</span>
	</br>
	<div class="btn-group">
		<%= link_to "Show house", feed_item, class: "btn btn-large btn-primary", user: current_user %>
		<%= link_to "Delete", feed_item, method: :delete, data: { confirm: "You sure?" }, title: feed_item.description, class: "btn btn-large btn-primary" %>
	</div>
</li>