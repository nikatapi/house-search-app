<script src="//maps.google.com/maps/api/js?v=3.13&amp;sensor=false&amp;libraries=geometry" type="text/javascript"></script>
<script src='//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js' type='text/javascript'></script>

<% provide(:title, @user.name) %>


<div class="row">
  <aside class="col-md-4">
    <section>
        <%= render 'shared/user_info' , :user => @user %>
    </section>

    <%= link_to "Search for a house!",  newsearch_path, class: "btn btn-large btn-primary" %>
    <%= link_to "Add A New House!",  newhouse_path, class: "btn btn-large btn-primary" %>
  </aside>


  <div class="col-md-8">
  	<%if @user.houses.any? %>
    	<h3>Registered houses (<%= @user.houses.count %>) </h3>
      
      <%= render 'shared/house_map' %>

    	<ul class ="houses">
    		<%= render @houses %> 
    	</ul>
      <%= will_paginate @houses %>
    <% else %>
      <h2> <%= @user.name %> doesn't have any registered houses at the moment</h2>
  	<% end %>
  </div>

  
</div>