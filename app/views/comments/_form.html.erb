<div class="comment-form">
	<%= form_for :comment, :url => newcomment_path, :remote => true do |f| %>
		<%= f.label "Write your review" %>
	  	<%= f.text_area :body, as: :text %>

	  	<%= f.hidden_field :commentable_id, :value => comment.commentable_id %>
	  	<%= f.hidden_field :commentable_type, :value => comment.commentable_type %>
	  	<div class="submit-btn">
	  	<%= f.button :submit, class: 'btn btn-primary', data: { disable_with: "Submitting…" } %>
	  	</div>

	<% end %>
</div>